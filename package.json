{
  "name": "teacher-app",
  "version": "1.0.0",
  "private": true,
  "description": "自由教师排课管理工具",
  "scripts": {
    "sync": "npx cap copy android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
